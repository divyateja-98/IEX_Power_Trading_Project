# Frontend

The frontend layer exposes the existing Streamlit dashboard through the
enterprise layout.

## Run

```powershell
streamlit run frontend/Home.py
```

The legacy dashboard page remains available under `dashboard/pages/`.
